#include <stdio.h>#include <stdlib.h>#include <math.h>typedef struct{	double x;	double y;}Point；int cluster_time=0;int data_size;int cluster_k;Point* p;Point* mean;int* cluster;int flag=1;void Initilize_Data();void Initilize_Mean();void UpdateCenter();void UpdateCluster();double getDistance(Point point1, Point point2);void MeanCopy(Point* mean_new);int main(int argc,char* argv[]){	data_size=strtol( argv[1],NULL,10);	cluster_k=strtol( argv[2],NULL,10);		p=malloc(data_size*sizeof(Point));	mean=malloc(cluster_k*sizeof(Point));	mean_copy=malloc(cluster_k*sizeof(Point));	cluster=malloc(data_size*sizeof(int));		Initilize_Data();	Initilize_Mean();		while(flag){		UpdateCluster();		UpdateCenter();		time++;	}	PrintResult();}void Initilize_Data(){	srand(unsigned time(NULL));	int i;	for(i=0;i<data_size;i++){		p[i].x=(double)rand()/RAND_MAX*5 -5;		p[i].y=(double)rand()/RAND_MAX*5 -5;	}}void Initilize_Mean(){	int i;	for(i=0;i<cluster_k;i++){		mean[i].x=p[i].x;		mean[i].y=p[i].y;		mean_copy[i].x=p[i].x;		mean_copy[i].y=p[i].y;	}}void UpdateCluster(){	int i,j;	double min;	double distance[data_size][cluster_k];	for(i=0;i<data_size;i++){		for(j=0;j<cluster_k;j++){			distance[i][j]=getDistance(point[i],mean[j]);		}		min=distance[i][0];		for(j=0;j<cluster_k;j++){			if (distance[i][j] <= min){                min = distance[i][j];                cluster[i] = j;            }		}		 printf("(%.0d,%.0d)\t in cluster-%d\n", p[i].x, p[i].y, cluster[i] + 1);	}}double getDistance(Point point1, Point point2){    double d;    d = sqrt((point1.x - point2.x)*(point1.x - point2.x) + (point1.y - point2.y)*(point1.y - point2.y));    return d;}void UpdateCenter(){	Point temp;	int i,j,count;	Point* mean_new=malloc(cluster_k*sizeof(Point));	for(j=0;j<cluster_k;j++){		temp.x=0.0;		temp.y=0.0;		count=0;		for(i=0;i<data_size;i++){			if (cluster[i] == j){                temp.x += p[i].x;                temp.y += p[i].y;                count++;            }		}		mean_new[j].x=sum.x/count;		mean_new[j].y=sum.y/count;	}	if(isEqual(mean_new,mean)){		flag=0;	}else {		MeanCopy(mean_new);	}}void MeanCopy(Point* mean_new){	int j;	for(j=0;j<cluster_k;j++){		mean[j].x=MeanCopy[j].x;		mean[j].y=MeanCopy[j].y;	}}